describe('Options tests', () => {
    /**@problem Готовим тестовое SQL подключение;
     */

    it('checking default options', async (done) => {
        const p = require('../../build/src/')
    });
});