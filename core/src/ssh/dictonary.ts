const path = require('path');
const { readdir, stat, readFile, writeFile } = require("fs").promises
/** @type {*} */
const sh_scripts = {
   install_nginx:require('./bash/ubuntu/install.nginx')
}
export class dictonary{
    getBashConf(AnyComand:String){
        
    }
}